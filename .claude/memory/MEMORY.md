# Memory index

- [Think/plan session style](think-plan-session-style.md) — session-scoped: no code/tests, short terse replies, no recap of known context (only when user declares the session as such)
- [Runtime core split](runtime-core-split.md) — deferred portable-runtime structure: Obsidian-free shared core, render-CSS-in-JS (single source) vs chrome CSS, two esbuild artifacts (main.js + lie-runtime.js); fold into AB7a/§3.6 after the crop-rework pass lands

- [LP float/wrap feasibility](lp-float-wrap-feasibility.md) — SHIPPED 2026-06-04: inline non-BFC float-escape makes lie-left/right wrap multi-line in LP (no desync/click-steal, image clickable via z-index:1); >250px tall-float edge handled by the tall-float cap
- [Image attr-list fallback prior art](img-attr-fallback-prior-art.md) — how python-markdown/Pandoc/kramdown handle `{…}` image attrs; validates style/class/data-attr split; kramdown needs `{:…}` colon (bare brace → literal text), Pandoc px→width/height attrs, Material uses `align=left/right` key=value
- [Tiny image toolbar diagnosis](tiny-image-toolbar-diagnosis.md) — RESOLVED 2026-06-04 (c192dcf): positionAbove now sits the bar ABOVE the image (rect.top−h−gap, below-fallback); float-out by coverage (0.6); shared JS body-float kept for all too-small cases (no paint-containment split — contain:paint blocks CSS-only). Fixtures permanent in examples vault.
- [LP rendering rework decisions](lp-rendering-rework-decisions.md) — IMPLEMENTED 2026-06-04 on main (final A/B'/C model): native image suppressed UNIFORMLY + our widget always renders (inline non-BFC for `{…}`, block:true for bare); NO normalization, NO `.lie-img` marker (both removed); reveal = auto/always + `<>` eye-dismiss; float in LP (z-index:1); tall-float cap (lie-tall + body.lie-safe-tall-float, LP+reader); shared isolateHistory writer (source-writer.ts). Folded into architecture.md/implementation-plan.md/test-plan.md/issues.md; standalone plan deleted.
- [Crop geometry rework](crop-geometry-rework.md) — 2026-06-04 Slices 1-4 done (uncommitted, user commits): 3-layer DOM (outer/.lie-frame/img), orientation=fields on the frame (centre pivot) → Bug 25 FIXED; crop serialization = transform= placement + aspect-ratio= cut shape (no px height); bare-key format (partial). Crop-in-place (Slice 5) DEFERRED; width/align migration + runtime bundle deferred.
