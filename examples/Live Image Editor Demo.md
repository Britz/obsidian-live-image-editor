# Live Image Editor — test & demo page

A self-contained note for screenshots and manual testing. Each image uses one of the
synthetic samples in `images/` (generated by `generate-samples.sh`). The corner
labels **A** (top-left), **B** (top-right), **C** (bottom-left), **D** (bottom-right)
and the **TOP** marker make rotation/flip easy to read. **Should** = what a correct
render must show. Transforms are stored as **native CSS** in the trailing `{…}` block.

> Open `examples/` as a vault (or copy this note + `images/` into your test vault)
> with the plugin enabled. Hover an image to reveal the toolbar.
## Original

**Image 1 — Original (landscape, unchanged).** *Should:* A top-left, B top-right, C/D bottom, **TOP** at the top, sun top-right. Full column width, no overflow.

![](images/sample-landscape.png){rotate=180 flip=horizontal transform="translate(-20.1%, -22.1%) rotate(0deg) scale(1)" filter="brightness(1.8)" width=200}

## Spacing (two images, no heading between)

**Images 1a & 1b — back-to-back images.** *Should:* exactly **6px** vertical gap between them (native parity), no extra space, no overlap.

![](images/sample-landscape.png){width=481}
![](images/sample-portrait.png){.lie-img style="width: 150px"}

## Rotate (quarter turns)

**Image 2 — 90° clockwise.** *Should:* **TOP** on the right (vertical, top→bottom), A top-right, B bottom-right, sun bottom-right. The box hugs the rotated image (no empty band top/bottom); width like a normal image.

![](images/sample-landscape.png){.lie-img style="transform: rotate(90deg)"}

**Image 3 — 180°.** *Should:* upside down — **TOP** at the bottom, A bottom-right, D top-left, sun bottom-left.

![](images/sample-landscape.png){.lie-img style="transform: rotate(180deg)"}

**Image 4 — 270° (90° counter-clockwise).** *Should:* **TOP** on the left (vertical), A bottom-left, B top-left, sun top-left. Box hugs the image like Image 2.

![](images/sample-landscape.png){.lie-img style="transform: rotate(270deg)"}

## Flip

**Image 5 — Flipped horizontally (portrait).** *Should:* **TOP** & arrow stay at the top, but left/right swapped: A top-**right**, B top-left; "PORTRAIT" reads mirrored.

![](images/sample-portrait.png){.lie-img style="transform: scaleX(-1)"}

**Image 6 — Flipped vertically (portrait).** *Should:* top/bottom swapped: **TOP** at the bottom, arrow points **down**, A bottom-left.

![](images/sample-portrait.png){.lie-img style="transform: scaleY(-1)"}

## Filters

**Image 7 — Grayscale.** *Should:* fully black & white, no colour.

![](images/sample-landscape.png){.lie-img style="filter: grayscale(1)"}

**Image 8 — Sepia.** *Should:* warm brownish tone (slightly raised contrast/brightness).

![](images/sample-landscape.png){.lie-img style="filter: sepia(0.8) contrast(1.05) brightness(1.05)"}

**Image 9 — Brighter + more saturated.** *Should:* visibly brighter and more colourful than Image 1 (brightness 1.3, saturate 1.6).

![](images/sample-landscape.png){.lie-img style="filter: brightness(1.3) saturate(1.6)"}

**Image 10 — Hue-rotated.** *Should:* colours shifted (hue-rotate 200° → the blue sky turns another colour), slightly desaturated.

![](images/sample-landscape.png){.lie-img style="filter: hue-rotate(200deg) saturate(0.9)"}

## Crop (native: box = cut frame; img = translate% + rotate + scale)

**Image 11 — Centre 500×500 of the square, shown at 250×250.** *Should:* the middle of the square with "SQUARE" **centred** (outer edges cut off on all sides), sharply clipped, **no** overflow past the cut edges.

![](images/sample-square.png){.lie-img style="width: 250px; height: 250px; transform: translate(-50%, -50%) scale(2)"}

## Resize (width, aspect kept)

**Image 12 — To 320px wide.** *Should:* smaller than Image 1, same aspect ratio.

![](images/sample-landscape.png){.lie-img style="width: 320px"}

## Preset classes (bundled example snippet — install/enable it)

**Image 13 — Circle mask (`.circle`), 200px.** *Should:* the square clipped to a **circle**.

![](images/sample-square.png){.lie-img .circle style="width: 200px"}

**Image 14 — Rounded + shadow (`.rounded .shadow`), 360px.** *Should:* rounded corners **and** a soft drop shadow.

![](images/sample-landscape.png){.lie-img .rounded .shadow style="width: 360px"}

**Image 15 — Border (`.bordered`), 200px portrait.** *Should:* a thin border all around.

![](images/sample-portrait.png){.lie-img .bordered style="width: 200px"}

## Combined (rotate + filter + size)

**Image 16 — 90° + sepia + 360px.** *Should:* the portrait sample lies **sideways** (rotated 90°), sepia-toned, box 360px wide, hugging (no empty band).

![](images/sample-portrait.png){.lie-img style="transform: rotate(90deg); filter: sepia(0.5); width: 360px"}

## Inline (mid-text icon)

An inline icon ![](images/sample-square.png){.lie-img .lie-inline style="height: 1.2em"} sits in the text line and flows with it.

## Edge: tiny toolbar cases

**Edge cases for the toolbar on a TINY image (24×24) — too short to hold the ~28px bar inside the image height.** *Should:* the toolbar appears on hover, fully visible and correctly positioned **above** the image, never clipped or sitting on/under it. These three cases decide whether the toolbar can be positioned purely in CSS (absolute overflow above) or still needs the JS body-float — the question is whether any wrapper is paint-contained so an above-overflow would be clipped.

**Case A — BARE tiny embed (no `{…}`, no surrounding text).** Bare-link path: Obsidian block-promotes the line, so the plugin draws its `block:true` overlay (a direct `.cm-content` child → `contain: paint`). *Should:* an above-overflow toolbar must not be clipped by `contain: paint`.

![](images/tiny-24.png)

**Case B — tiny embed WITH `{…}`.** The trailing `{…}` keeps the line a real `.cm-line`, so the plugin renders an inline widget (NOT paint-contained). *Should:* the toolbar can overflow visibly above the image — no body-float needed.

![](images/tiny-24.png){style="width: 24px"}

**Case C — inline icon mid-text (`.lie-inline`).** An inline tiny icon ![](images/tiny-24.png){.lie-inline style="width: 22px"} sits within this sentence and flows with the text; its wrapper is an inline replace-widget inside the `.cm-line`. *Should:* the same above-overflow toolbar must not be clipped.
